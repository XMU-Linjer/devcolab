# Vite development server only. Production uses the Nginx same-origin routes.
VITE_DEV_PORT=5173
VITE_API_PROXY_TARGET=http://localhost:8080
VITE_WS_PROXY_TARGET=ws://localhost:8090

# Keep false until the backend implements /api/v1/landing/*.
VITE_LANDING_DATA_API_ENABLED=false

server:
  port: ${DEVCOLLAB_GATEWAY_PORT:8090}

spring:
  application:
    name: devcollab-collaboration-gateway
  data:
    redis:
      host: ${DEVCOLLAB_REDIS_HOST:localhost}
      port: ${DEVCOLLAB_REDIS_PORT:6379}

devcollab:
  gateway:
    core-transport: ${DEVCOLLAB_GATEWAY_CORE_TRANSPORT:grpc}
    core-base-url: ${DEVCOLLAB_CORE_BASE_URL:http://localhost:8080}
    presence-ttl: ${DEVCOLLAB_GATEWAY_PRESENCE_TTL:PT1M}
    editing-ttl: ${DEVCOLLAB_GATEWAY_EDITING_TTL:PT1M}
    operation-dedup-ttl: ${DEVCOLLAB_GATEWAY_OPERATION_DEDUP_TTL:PT5M}
    core-grpc:
      host: ${DEVCOLLAB_CORE_GRPC_HOST:localhost}
      port: ${DEVCOLLAB_CORE_GRPC_PORT:9090}
      plaintext: ${DEVCOLLAB_CORE_GRPC_PLAINTEXT:true}
      deadline: ${DEVCOLLAB_CORE_GRPC_DEADLINE:PT3S}
      max-inbound-message-size: ${DEVCOLLAB_CORE_GRPC_MAX_INBOUND_MESSAGE_SIZE:1048576}
      shutdown-timeout: ${DEVCOLLAB_CORE_GRPC_SHUTDOWN_TIMEOUT:PT5S}
  security:
    jwt:
      issuer: ${DEVCOLLAB_JWT_ISSUER:devcollab-knowledge-core}
      audience: ${DEVCOLLAB_JWT_AUDIENCE:devcollab-web}
      secret: ${DEVCOLLAB_JWT_SECRET:devcollab-local-development-secret-change-me}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        devcollab.core.grpc.client.duration: true
  tracing:
    enabled: ${DEVCOLLAB_TRACING_ENABLED:false}
    sampling:
      probability: ${DEVCOLLAB_TRACING_SAMPLING_PROBABILITY:1.0}
  otlp:
    tracing:
      endpoint: ${DEVCOLLAB_OTLP_TRACES_ENDPOINT:http://localhost:4318/v1/traces}

logging:
  pattern:
    correlation: "[${spring.application.name:devcollab-collaboration-gateway},%X{traceId:-},%X{spanId:-}] "
